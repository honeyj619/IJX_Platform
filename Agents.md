# IJX Platform Memory

## 1. 项目定位

* 本项目为 IJX Platform 前端原型项目，当前阶段以页面原型、前端交互和 mock 数据演示为主。
* 默认不接入真实后端接口，除非用户明确要求。
* 页面实现应服务于企业级协同办公、如意空间、管理后台、下载页等场景，保持统一的产品体系和视觉风格。

## 2. 工作目录与提交流程

* 开发提交目录以 `D:\Codex\github-ijx-platform-im-web\work\push\IJX_Platform` 为准。
* 预览目录以 `D:\Codex\github-ijx-platform-im-web\work\preview\IJX_Platform-main` 为准。
* 修改完成后必须先运行构建，确认无白屏、无编译错误、无明显乱码。
* 需要预览时，应同步到 preview 目录，并给出可访问地址。
* 未经用户明确确认，不自动推送 git。
* 用户明确要求推送时，提交到 `main` 分支。
* 推送 `main` 后，GitHub Actions 会自动触发 GitHub Pages 更新。
* 每次推送后的回复必须包含“功能路由 + 简述”清单。

## 3. 技术栈

* 核心技术栈：React 18、TypeScript、Vite、Tailwind CSS。
* 路由使用 `react-router-dom`。
* 状态管理优先使用现有 Zustand store 或页面内 state。
* 图标优先使用 `lucide-react`。
* 样式优先使用 Tailwind CSS，避免大量自定义 CSS，除非 Tailwind 难以表达。

## 4. 目录结构与文件组织

* `src/App.tsx`：全局路由入口。
* `src/components/`：可复用组件、布局、导航、通用 UI。
* `src/pages/`：页面级组件。
* `src/data/`：全局 mock 数据、演示人名、头像等共享数据。
* `src/store/`：全局状态。
* `src/constants/`：常量配置。
* `src/hooks/`：通用 hooks。
* `src/assets/`：静态资源。

新增内容应遵循以下规则：

* 新增页面优先放在 `src/pages/`。
* 新增跨页面复用能力优先放在 `src/components/`、`src/data/`、`src/constants/`。
* 不要在多个页面里重复写相同 mock 数据、常量或组件逻辑。
* 页面内只被当前页面使用的小组件，可以写在同一个页面文件底部。
* 不要把具体一次性产品需求写入 `MEMORY.md` 或 `AGENTS.md`。

## 5. 命名规范

* 页面组件使用 PascalCase，例如 `WorkReport.tsx`、`OkrModule.tsx`。
* 通用组件使用 PascalCase，并放在 `src/components/`。
* 变量、函数、状态使用 camelCase。
* mock 数据集合使用语义化名称，例如 `reportItems`、`okrObjectives`、`businessSystems`。
* 不要新增含义模糊的组件名，例如 `Box1`、`NewCard`、`TempPanel`。

## 6. 路由规则

* Web 客户端页面统一挂在 `/web_client` 下。
* `/admin` 是独立后台页面。
* `/download` 是独立下载页。
* `/admin`、`/download` 不挂在 `/web_client` 下。
* 旧路由可保留重定向到 `/web_client/...`，避免历史地址失效。
* 新增 Web 客户端页面时，需要同时检查：
  * `src/App.tsx` 路由配置；
  * 入口按钮或导航跳转路径；
  * GitHub Pages basename 兼容性。
* 不要把后台页面、下载页嵌入客户端 Layout。

## 7. 编码与文本规范

* 文件统一使用 UTF-8，避免引入 BOM。
* 写入文件时优先保持原文件编码和换行风格。
* 修改中文文案后必须检查乱码。
* 不使用破损字符、问号占位或编码异常文本。
* 不因为批量替换扩大无关改动。
* 如需清理格式，只处理与当前任务相关的文件。
* 不做大范围批量替换，除非用户明确要求。
* 注释只写必要说明，不写空泛解释。

## 8. CSS 与视觉实现规范

### 8.1 通用视觉原则

* 复用现有色彩、圆角、阴影、间距规则。
* 页面应保持企业级、清爽、克制、可维护的视觉风格。
* 避免无意义渐变、装饰图形、大面积高饱和背景。
* 避免过重阴影、霓虹发光、过度科技感。
* 文本、按钮、浮层、菜单不能互相遮挡。
* 不要嵌套卡片套卡片，除非是明确的列表项、弹框内容或工具容器。

### 8.2 不同页面风格

* 管理后台必须保持统一后台风格，不随外部截图改变视觉体系。
* 工作门户、如意空间可更轻盈，但必须保持品牌一致。
* 图标按钮优先使用 `lucide-react` 图标。
* 配置类、列表类、管理类页面应优先使用清晰的信息层级、统一卡片和标准操作区。

### 8.3 响应式要求

* 窄屏时优先使用折行、收缩、区域滚动。
* 不要让整体页面出现不必要的横向滚动。滚动条在鼠标悬浮时才展示，页面不能同时展示太多滚动条。
* 表格、卡片、导航、弹窗在窄屏下必须保持可读和可操作。

## 9. Mock 数据规范

* 主用户信息统一从 `src/data/people.ts` 引用。
* 主用户固定为 `MAIN_USER_NAME`，即梁吉力。
* 主用户头像统一使用 `MAIN_USER_AVATAR`。
* 其他演示用户从 `THREE_KINGDOMS_NAMES` 或 `getDemoPerson()` 获取。
* 不随意新增真实姓名、手机号、邮箱等个人信息。
* 不新增真实人员隐私数据。
* 多页面共用的 mock 数据应抽到 `src/data/` 或 `src/constants/`。
* 页面私有 mock 数据可以保留在页面文件内，但命名必须清晰。
* mock 数据要能支撑当前交互状态，例如：

  * 空状态；
  * 选中态；
  * 已提交；
  * 未提交；
  * 禁用态；
  * 加载态；
  * 异常态。

## 10. 交互与状态规则

* 新增交互必须有可见反馈，例如 toast、弹框、选中态、状态切换。
* 删除、清空、覆盖类操作必须有确认弹框。
* 配置类操作优先使用弹框或抽屉，不要直接静默修改。
* 列表筛选、卡片选中、详情展示要有明确联动关系。
* 对话类页面中，每次用户操作应生成新的对话消息。
* 不要在旧消息内部嵌套推进新的对话流程。
* 如意助手回复必须保持统一对话消息样式。

## 11. 验证流程

修改后至少运行：

```bash
npm.cmd run build
```

如果涉及类型、路由、组件导入，优先补充运行：

```bash
npm.cmd run check
```

涉及页面交互或视觉调整时，需要：

* 同步到 preview 目录；
* 确认页面可访问；
* 检查无白屏、无明显布局错乱、无乱码；
* 向用户提供预览地址。

推送前必须：

* 确认 `git status`；
* 检查是否混入无关改动；
* 确认构建通过；
* 确认用户已明确要求推送。

## 12. 禁止改动范围

未经用户明确要求，不得执行以下操作：

* 自动推送 git；
* 修改 GitHub Actions；
* 修改部署配置；
* 修改仓库远端配置；
* 删除用户已有改动；
* 使用 `git reset --hard`；
* 使用 `git checkout --` 回退文件；
* 重构无关页面；
* 改动 `node_modules`；
* 改动构建产物 `dist`；
* 提交构建产物；
* 新增真实人员隐私数据；
* 将一次性产品需求写入 `MEMORY.md` 或 `AGENTS.md`。
