# IJX Platform Memory

## 项目定位
- 新协同平台定位为公司级协同办公入口，覆盖 Web、App、PC 多端，核心目标是统一入口、统一待办、统一消息、统一智能协同能力，提升员工日常办公效率。
- 本项目是企业级智能办公平台前端原型，面向内部员工日常办公，重点验证功能流程、信息架构、交互体验和视觉规范。
- 当前阶段以纯前端 mock 为主，不要求真实后端接口；实现时优先保证页面可预览、流程可理解、状态可演示。
- 产品气质应清晰、高效、克制、可扫描，首屏优先展示真实可用功能，不做营销页、展示页或炫技页。

## 设计总原则
- 用户给的截图只参考功能结构、字段和交互意图，不直接复制外部视觉风格；最终页面必须服从当前系统统一设计规范。
- 同一系统内的卡片、弹框、按钮、列表、筛选、导航要保持一致，避免一个页面一套风格或临时拼贴式 UI。
- 企业办公页面以信息清晰和操作效率为优先，复杂流程要体现阶段关系，但不要做成难以理解的装饰。
- 页面要适配主流笔记本屏幕，窄屏下允许合理折行、收缩或区域滚动；文本、按钮、浮层、菜单不能互相遮挡。
- 每次新增或调整页面时，需要先判断页面所属模块和用户场景，再复用现有设计语言。输出时说明：新增了什么页面、后续可扩展点是什么。不要大范围重构已有页面，除非用户明确要求。

## 色彩视觉
- 主品牌色以吉祥航空玫红、深红体系为核心；辅助色可使用蓝、绿、橙等状态色，但不能喧宾夺主。
- 管理后台保持统一后台视觉，不跟随外部参考截图变化；工作门户、如意空间可更轻盈，但仍需和整体品牌一致。
- 避免大面积单一紫色、米色、暗蓝、咖啡色或过度渐变；卡片阴影、圆角、边框、背景层级要克制统一。
- 重点内容用颜色、间距、层级和空间关系表达，不依赖大面积高饱和背景或无意义装饰。

## 组件规范
- 常见操作优先使用图标按钮，图标优先使用 lucide；不要用不可解释的装饰图形占据功能空间。
- 卡片右上角操作、弹框标题/内容区/底部按钮、列表筛选/详情/选中态要形成统一组件规则。
- 主操作与次操作顺序要稳定，选中态、禁用态、hover 态、空状态都要设计完整。
- 悬浮菜单默认展示在目标元素外侧，空间不足时再收进内部；滚动条尽量在需要时出现，避免破坏页面整洁。
- 不要嵌套卡片套卡片，除非是明确的列表项、弹框内容或工具容器。

## 如意空间规范
- AI 能力应围绕如意空间和如意助手形成统一入口。
- 如意空间是协同 AI 的统一入口，承载插件选择、智能体调用、知识调用、接口调用和智能体广场。
- 如意助手是企业级系统助手，是员工主要对话入口，可调用企业知识库、协同系统接口、mcp能力等。新增设计时，作为协同办公助手，考虑整个功能在企业内部的连通，可增加相应设计。
- 如意助手选择插件后，不展示通用猜你想问。对话页是 IM 式体验，对话框固定在内容区底部，助手回复必须保持统一对话消息样式，不做复杂页面模块。
- 每次用户点击操作，应产生新的对话消息，不在旧消息内嵌套推进。
- 常用助手与历史对话要有明确区分；历史对话以对话名称为主，如果来自某助手，用小字展示助手名。
- 新版、旧版如意助手入口不能遮挡页面核心内容；跳转其他功能时，尽量保持在内容区内切换，避免破坏导航上下文。

## 管理后台规范
- 管理后台必须使用统一后台组件规范；外部截图只参考功能，不继承外部颜色、间距、字体和组件风格。
- 左侧导航、顶部标签、面包屑、查询区、表格区、详情页、弹框、抽屉的结构和操作位置必须统一。
- 一级分类默认收起，不默认展开具体菜单；表格页保持后台信息密度，避免卡片化过度。
- 编辑页字段布局要对齐，必填、说明、上传、预览等模式要统一。
- 后台页面宽度跟随容器适配，不能出现不必要的横向滚动；同类管理页面优先复用现有结构，不新写一套视觉。

## 人名和头像
- 主用户固定为梁吉力；主用户名称、简称、头像从 `src/data/people.ts` 统一引用。
- 页面中出现梁吉力时，头像必须与主用户头像一致；需要头像时，主用户用 `MAIN_USER_AVATAR`，其他用户可用 `getInitialsAvatar`。
- 其他演示用户从 `THREE_KINGDOMS_NAMES` 中选择，不随意新增真实姓名，避免不同页面人名体系混乱。